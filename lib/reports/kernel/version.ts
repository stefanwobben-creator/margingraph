/**
 * The kernel's own version.
 *
 * Bump the minor when a reasoner is added or its output shape grows; bump the
 * major when an existing reasoner's scoring changes, because that changes the
 * confidence attached to reports already sold.
 */
export const KERNEL_VERSION = "0.1.0";
